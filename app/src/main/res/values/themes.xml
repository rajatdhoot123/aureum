<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.RateWatch" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="colorPrimary">@color/gold_primary</item>
    </style>

    <!-- Splash theme for cold start -->
    <style name="Theme.RateWatch.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/icon_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_ratewatch_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.RateWatch</item>
    </style>
</resources>
